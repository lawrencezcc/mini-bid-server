// use username/pwd as auth
// read basic jira info
// getStoryPoints?
// getAllTickets of mini bid of an user
'use strict';

const getTicketBrief = (ticketNum = NaN) => {
    if (ticketNum !== NaN) {
        // get jira ticket info
        // title, description, comments, assignee, reporter, end time, status
    }
};

const getStoryPoints = (ticketNum = NaN) => {
    if (ticketNum !== NaN) {
        // get the story points
    }
};

/*
** get all mini-bid tickets of an user
*/
const getAllticketsByUser = (email) => {}
